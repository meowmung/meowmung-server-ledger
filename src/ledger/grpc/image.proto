syntax = "proto3";

package hero;

service HeroesService {
    rpc FindOne (ParamImage) returns (Result);
}

message ParamImage {
    int32 id = 1;
}

message Result {
    int32 id = 1;
    string name = 2;
}